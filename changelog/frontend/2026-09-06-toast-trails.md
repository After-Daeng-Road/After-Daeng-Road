# [frontend] 공통 토스트 시스템 · 두루누비 산책로 탭 · 인트로 레이아웃 픽스

- **날짜**: 2026-09-06
- **브랜치**: `feature/toast-trails` (← `dev`)
- **담당**: 프론트엔드
- **상태**: dev 병합 대기

## 공통 토스트

- `components/ui/toast.tsx` — ToastProvider + `useToast()` 훅. 상단 중앙(헤더 아래) 스택,
  흰 필 + line 보더, lucide 아이콘(기본=브랜드 발바닥/성공=세이지 체크/실패=로즈 경고),
  2.8초 자동 소멸
- 배선: 추천받기 비로그인 클릭(버튼 활성 + "로그인 후 이용" 토스트, 로그인 시 펫 미등록만
  비활성), 로그인/로그아웃 전환(`auth-toast-watcher` — sessionStorage 상태 비교로
  리로드 넘어 감지), 후기 등록(인라인 메시지 → 토스트 전환), 북마크 저장/해제/실패

## 두루누비 산책로 (`/trails`)

- GNB 추천-마이펫타임 사이 '산책로' 탭 신설 (헤더 slice(0,3)→(0,4) — 항목 추가 시
  마이펫타임이 밀려 사라지던 문제 수정)
- 16개 코스(서해랑길) 카드 그리드: 테마·시군, 난이도 칩(두루누비 crsLevel 1~3 →
  쉬움/보통/힘듦 신호등 점), 설명 2줄, 거리·소요·고도 스탯, 카카오 길찾기
- 16건 고정 데이터 → `revalidate = 86400` 하루 캐시. sitemap 에 /trails 추가
- 참고: 두루누비 POI 추천 후보 편입(content_type_id NULL·sigungu 백필)은 백엔드 전달

## 인트로 레이아웃 픽스

- 영상에 세로 상한 `max-h-[78vh]` — 16:9 화면(1920×1080 100%)에서 가로 기준으로만
  커져 하단 '오늘 하루 보지 않기/건너뛰기'가 짤리던 문제 해결

## 검증

- tsc·eslint 클린, /trails 200 + 16카드 렌더 확인(브라우저), 홈·상세 200
